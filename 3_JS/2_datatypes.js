let name = "supriya"; // string
let age = 20; // number
let canVote = true; // boolean
let isRed; // undefined
const student = {
  // object
  name: "supriya",
  age: 20,
};
const colors = ["red", "blue", "green"]; // Array object
const date = new Date("2025-01-01"); // date object

console.log(name, age, canVote, isRed, student, colors, date);
